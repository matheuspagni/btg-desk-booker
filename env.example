# Configuração do PostgreSQL (RDS)
# Copie este arquivo para .env.local e preencha com as suas credenciais
DATABASE_HOST=desk-booker-db.c968iysiwb9m.sa-east-1.rds.amazonaws.com
DATABASE_PORT=5432
DATABASE_NAME=deskbooker
DATABASE_USER=matheuspagni
DATABASE_PASSWORD=sua-senha-aqui
# Schema padrão para desenvolvimento local; em produção ajustar para "prod"
DATABASE_SCHEMA=dev
# DATABASE_SCHEMA=prod
# Se a instância exigir SSL (RDS público recomenda), deixe como true
DATABASE_SSL=true

# Opcional: conexão única via URL (sobrescreve os campos acima)
# DATABASE_URL=postgresql://matheuspagni:sua-senha-aqui@desk-booker-db.c968iysiwb9m.sa-east-1.rds.amazonaws.com:5432/deskbooker
